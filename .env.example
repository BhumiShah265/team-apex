# Krishi-Mitra AI - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================
# EMAIL CONFIGURATION (Optional - for Password Reset)
# ============================================================
# For production, set these values to enable real email sending
# In simulation mode (console), OTPs will be printed to console

# SMTP Server (e.g., smtp.gmail.com)
SMTP_SERVER=smtp.gmail.com

# SMTP Port (usually 587 for TLS)
SMTP_PORT=587

# Sender Email Address
SENDER_EMAIL=your-email@gmail.com

# Sender App Password (not regular password - generate from Google Account)
# For Gmail: https://myaccount.google.com/apppasswords
SENDER_PASSWORD=your-app-password

# Enable Console Mode for Testing (true/false)
# When true, OTPs are printed to console instead of sending emails
EMAIL_CONSOLE_MODE=true

